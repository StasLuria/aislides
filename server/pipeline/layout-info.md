# Layout Reference

## Available Layouts (18 total)
1. title-slide: title, description, presenterName, initials, presentationDate, image{url,alt}
2. section-header: title, subtitle
3. text-slide: title, bullets[{title,description}], icon{name,url}
4. two-column: title, leftColumn{title,bullets[]}, rightColumn{title,bullets[]}
5. image-text: title, bullets[{title,description}], image{url,alt}
6. image-fullscreen: title, subtitle, backgroundImage{url,alt}
7. quote-slide: quote, author, role, backgroundImage{url,alt}
8. chart-slide: title, description, chartData{type,labels[],datasets[{label,data[],backgroundColor}]}
9. table-slide: title, description, headers[], rows[][]
10. icons-numbers: title, metrics[{label,value,description,icon}]
11. timeline: title, events[{date,title,description}]
12. process-steps: title, steps[{number,title,description}]
13. comparison: title, optionA{title,points[],color}, optionB{title,points[],color}
14. final-slide: title, subtitle, thankYouText, contactInfo[{type,value}]
15. agenda-table-of-contents: title, sections[{number,title,description}]
16. team-profiles: title, companyDescription, teamMembers[{name,role,description,image{url,alt}}]
17. logo-grid: title, description, logos[{name,image{url,alt}}]
18. video-embed: title, description, videoUrl, thumbnailImage{url,alt}
